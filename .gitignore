# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# Logs / temp
*.log
*.tmp
*.bak
.tmp/
.cache/

# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*.pfx
secrets.*
.secrets/

# Local actionlint / tooling output
actionlint
actionlint.exe
.actionlint-cache/

# Python (if scripts are added later)
__pycache__/
*.py[cod]
.venv/
venv/

# Node (if scripts are added later)
node_modules/
npm-debug.log*

# Misc
coverage/
dist/
build/

# macOS resource forks (created on non-HFS volumes / network shares).
._*
.AppleDouble
.LSOverride

# Merge / patch conflict artifacts.
*.orig
*.rej

# direnv — environment loaders are per-developer.
.envrc
.direnv/


# >>> managed-file-sync:common >>>
# Managed by Blackout Secure Managed File Sync — configure services in bos-universal-config.json.
# OS + editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Build + dependency output
node_modules/
dist/
build/
coverage/
__pycache__/
*.py[cod]
.venv/

# Local secrets (never commit these)
.env
.env.*
!.env.example
# <<< managed-file-sync:common <<<
